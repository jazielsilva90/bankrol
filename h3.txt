reiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];function calendar(list){const y=calDate.getFullYear(),m=calDate.getMonth(),first=new Date(y,m,1),start=new Date(y,m,1-first.getDay()),group={};list.forEach(r=>(group[r.date]??=[]).push(r));let total=0,pos=0,neg=0,neu=0,days=[];for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);const k=d.toISOString().slice(0,10),items=group[k]||[],done=items.filter(r=>r.result!=='pending'),v=done.reduce((a,r)=>a+num(r.impactScore),0);if(d.getMonth()===m){total+=v;v>0?pos++:v<0?neg++:neu++}const st=stats(items),markets=[...new Set(items.map(r=>r.market).filter(Boolean))];days.push({d,k,v,items,st,markets,out:d.getMonth()!==m,today:k===new Date().toISOString().slice(0,10)})}return`<div class="calendar-head"><div><div class="ey">CALENDÁRIO</div><h3>Calendário de resultados</h3></div><div class="calendar-controls"><button id="calPrev" class="btn secondary">‹</button><div class="calendar-month">${MONTHS[m]} de ${y}</div><button id="calNext" class="btn secondary">›</button><button id="calToday" class="btn secondary">Hoje</button></div></div><div class="calendar-week">${['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'].map(x=>`<span>${x}</span>`).join('')}</div><div class="calendar-grid">${days.map(o=>`<div class="calendar-day ${o.out?'out ':''}${o.today?'today ':''}${o.v>0?'positive':o.v<0?'negative':''}" data-day='${JSON.stringify({date:o.k,count:o.items.length,success:o.st.success,failure:o.st.failure,pending:o.st.pending,impact:o.v,roi:o.st.roi,rate:o.st.rate,markets:o.markets}).replace(/'/g,'&#39;')}'><span class="day-number">${o.d.getDate()}</span><span class="day-value ${o.v>0?'pos':o.v<0?'neg':''}">${o.v?money(o.v):''}</span></div>`).join('')}</div><div class="calendar-summary"><div class="legend"><span><i style="background:var(--g)"></i>Positivo ${pos}</span><span><i style="background:var(--r)"></i>Negativo ${neg}</span><span><i style="background:#64748b"></i>Neutro ${neu}</span></div><div class="calendar-total"><small>Total do mês</small><strong class="${total>=0?'pos':'neg'}">${money(total)}</strong></div></div><div id="dayTooltip" class="day-tooltip"></div><div id="chartTooltip" class="chart-tooltip"></div>`}function rank(list,key){const m={};list.forEach(r=>{const k=key(r)||'Não informado';m[k]??={name:k,count:0,wins:0,losses:0,pending:0,impact:0,weight:0,odds:0,oddsCount:0};m[k].count++;m[k].wins+=r.result==='success';m[k].losses+=r.result==='failure';m[k].pending+=r.result==='pending';m[k].impact+=num(r.impactScore);m[k].weight+=num(r.analysisWeight);if(num(r.confidenceIndex)){m[k].odds+=num(r.confidenceIndex);m[k].oddsCount++}});return Object.values(m).map(x=>({...x,rate:(x.wins+x.losses)?x.wins/(x.wins+x.losses)*100:0,roi:x.weight?x.impact/x.weight*100:0,avgOdds:x.oddsCount?x.odds/x.oddsCount:0})).sort((a,b)=>b.count-a.count||b.impact-a.impact)}function insightCard(title,rows,type='generic',wide=false){const original=rows||[],state=dashSort[type]||{key:null,dir:0},value=(x,key)=>key==='name'?String(x.name||'').toLocaleLowerCase('pt-BR'):key==='count'?x.count:key==='rate'?x.rate:key==='avgOdds'?x.avgOdds:key==='impact'?x.impact:0,list=[...original];if(state.key&&state.dir){list.sort((a,b)=>{const av=value(a,state.key),bv=value(b,state.key);let c=typeof av==='string'?av.localeCompare(bv,'pt-BR'):av-bv;return c*state.dir});}const leader=[...original].sort((a,b)=>b.impact-a.impact||b.rate-a.rate)[0]||null,labels={sport:['Esportes','Destaque'],platform:['Plataformas','Melhor plataforma'],competition:['Competições','Melhor competição'],analyst:['Analistas','Analista destaque'],category:['Categorias','Melhor categoria'],generic:['Grupos','Destaque']}[type]||['Grupos','Destaque'],head=(label,key)=>`<span class="dash-sortable ${state.key===key?'active':''}" title="Clique para ordenar" data-dash-type="${type}" data-dash-sort="${key}">${label}<i class="dash-sort-ind">${state.key===key?(state.dir===1?'↑':'↓'):''}</i></span>`;return`<article class="card insight-card dashboard-center ${wide?'wide-insight':''}"><div class="card-head"><div><div class="ey">DASHBOARD</div><h3>${title}</h3></div></div><div class="dimension-summary"><div><small>${labels[0]}</small><strong>${original.length}</strong></div><div><small>${labels[1]}</small><strong>${esc(leader?.name||'—')}</strong></div><div><small>Retorno destaque</small><strong class="${(leader?.impact||0)>=0?'pos':'neg'}">${money(leader?.impact||0)}</strong></div><div><small>Odds média destaque</small><strong>${quote(leader?.avgOdds||0)}</strong></div></div><div class="dimension-pills"><div class="result-pill win">✓ ${leader?.wins||0} certas</div><div class="result-pill loss">× ${leader?.losses||0} erradas</div><div class="result-pill wait">⌛ ${leader?.pending||0} pendentes</div></div><div class="dimension-meta"><div><b>${pct(leader?.rate||0)}</b><small>Win rate destaque</small></div><div><b>${leader?.count||0}</b><small>Análises destaque</small></div><div><b class="${(leader?.roi||0)>=0?'pos':'neg'}">${pct(leader?.roi||0)}</b><small>ROI destaque</small></div></div><div class="insight-table-head">${head(type==='category'?'Categoria':type==='platform'?'Plataforma':type==='competition'?'Competição':type==='analyst'?'Analista':'Item','name')}${head('Qtd','count')}${head('Acerto','rate')}${head('Odd média','avgOdds')}${head('Retorno','impact')}</div><div>${list.length?list.slice(0,7).map(x=>`<div class="insight-row"><span class="name">${esc(x.name)}</span><span class="qty">${x.count}</span><span class="hit">${x.wins}/${x.wins+x.losses}</span><span class="odds">${quote(x.avgOdds)}</span><span class="return ${x.impact>=0?'pos':'neg'}">${money(x.impact)}</span></div>`).join(''):'<div class="muted" style="text-align:center">Sem dados suficientes.</div>'}</div></article>`}function bindDashboardSort(){$$('[data-dash-sort]').forEach(el=>el.onclick=()=>{const type=el.dataset.dashType,key=el.dataset.dashSort,current=dashSort[type]||{key:null,dir:0};if(current.key!==key)dashSort[type]={key,dir:-1};else if(current.dir===-1)dashSort[type]={key,dir:1};else delete dashSort[type];render()})}function bindCalendarTips(){const tip=$('#dayTooltip');if(!tip)return;$$('[data-day]').forEach(el=>{const show=e=>{let d;try{d=JSON.parse(el.dataset.day)}catch{return}const no=d.count===0;tip.innerHTML=`<h4>${d.date.split('-').reverse().join('/')}</h4>${no?'<div class="muted">Nenhuma análise registrada neste dia.</div>':`<div class="tip-grid"><span>Análises</span><b>${d.count}</b><span>Certas / Erradas / Pendentes</span><b>${d.success} / ${d.failure} / ${d.pending}</b><span>Retorno</span><b class="${d.impact>=0?'pos':'neg'}">${money(d.impact)}</b><span>ROI</span><b>${pct(d.roi)}</b><span>Taxa de acerto</span><b>${pct(d.rate)}</b></div>`}`;const x=Math.min(innerWidth-290,e.clientX+14),y=Math.min(innerHeight-220,e.clientY+14);tip.style.left=x+'px';tip.style.top=y+'px';tip.classList.add('show')};el.addEventListener('mousemove',show);el.addEventListener('mouseenter',show);el.addEventListener('mouseleave',()=>tip.classList.remove('show'));el.addEventListener('click',e=>{show(e);setTimeout(()=>tip.classList.remove('show'),2600)})})}function dash(){const list=range(),s=stats(list),donut=`conic-gradient(var(--g) 0 ${s.rate}%,#263532 ${s.rate}% 100%)`,categories=rank(list,r=>r.market);ticker(list);metrics(list);$('#content').innerHTML=`<div class="dash-topline"><div class="dash-period"><select id="range"><option value="all">Todo o período</option><option value="7">Últimos 7 dias</option><option value="30">Últimos 30 dias</option><option value="90">Últimos 90 dias</option></select></div></div><section class="dashboard-grid"><article class="card chart-card"><div class="card-head"><div><div class="ey">EVOLUÇÃO</div><h3>Retorno acumulado</h3></div><div class="chart-tabs"><button class="chart-tab ${chartMode==='accum'?'on':''}" data-chart="accum">Acumulado</button><button class="chart-tab ${chartMode==='daily'?'on':''}" data-chart="daily">Diário</button><button class="chart-tab ${chartMode==='monthly'?'on':''}" data-chart="monthly">Mensal</button></div></div>${chart(list)}</article><article class="card performance-card"><div class="card-head"><div><div class="ey">RESULTADOS</div><h3>Desempenho</h3></div></div><div class="performance-body"><div class="donut" style="background:${donut}"><div><strong>${pct(s.rate)}</strong><small>Taxa de acerto</small></div></div><div class="counts"><div><strong class="pos">${s.success}</strong><small>Acertos</small></div><div><strong class="neg">${s.failure}</strong><small>Erros</small></div><div><strong class="amber">${s.pending}</strong><small>Pendentes</small></div></div><div class="pstats"><div><strong>${quote(avg(list,r=>r.confidenceIndex))}</strong><small>Cotação média</small></div><div><strong>${money(avg(list,r=>r.analysisWeight))}</strong><small>Valor médio</small></div><div><strong class="pos">+${bestStreak(list)}</strong><small>Melhor sequência</small></div></div></div></article></section><section class="dashboard-grid"><article class="card calendar-card">${calendar(list)}</article><article class="card category-card">${insightCard('Análise por categoria',categories,'category').replace(/^<article class="card insight-card[^>]*>|<\/article>$/g,'')}</article></section><section class="insight-grid">${insightCard('Análises por esporte',rank(list,r=>r.sport),'sport')}${insightCard('Plataforma de referência',rank(list,r=>r.platform),'platform')}${insightCard('Competição',rank(list,r=>r.competition),'competition')}${insightCard('Analista',rank(list,r=>r.analyst),'analyst')}</section>`;$('#range').value=dashRange;$('#range').onchange=e=>{dashRange=e.target.value;render()};$$('[data-chart]').forEach(b=>b.onclick=()=>{chartMode=b.dataset.chart;render()});$('#calPrev').onclick=()=>{calDate.setMonth(calDate.getMonth()-1);render()};$('#calNext').onclick=()=>{calDate.setMonth(calDate.getMonth()+1);render()};$('#calToday').onclick=()=>{calDate=new Date();render()};bindCalendarTips();bindChartTips();bindDashboardSort()}function analysisCardInner(rows){const max=Math.max(1,...rows.map(x=>Math.abs(x.impact)));return rows.length?`<div class="mini-head"><span>Categoria</span><span>Distribuição</span><span>Retorno</span><span>ROI</span></div><div class="analysis-list">${rows.slice(0,7).map((x,i)=>`<div class="analysis-row"><b>${esc(x.name)}</b><div class="bar ${x.impact<0?'orange':''}"><span style="width:${Math.max(5,Math.abs(x.impact)/max*100)}%;background:${x.impact<0?'var(--r)':'var(--g)'}"></span></div><em class="${x.impact>=0?'pos':'neg'}">${money(x.impact)}</em><em>${pct(x.count?x.impact/Math.max(1,x.count):0)}</em></div>`).join('')}</div>`:'<div class="empty">Cadastre análises para gerar este painel.</div>'}function setupAutocomplete(form){const fields=['sport','competition','homeTeam','awayTeam','mainTeam','market','strategy','platform','analyst'];const key=v=>String(v||'').trim().toLocaleLowerCase('pt-BR');fields.forEach(name=>{const input=form.elements[name];if(!input)return;input.setAttribute('autocomplete','off');const wrap=input.closest('.field'),menu=document.createElement('div');menu.className='autocomplete-menu';wrap.appendChild(menu);let active=-1,opts=[];const pool=()=>{const freq=new Map,selectedSport=key(form.elements.sport?.value);records.forEach(r=>{if(['competition','homeTeam','awayTeam','mainTeam'].includes(name)&&selectedSport&&key(r.sport)!==selectedSport)return;const values=name==='mainTeam'?[r.mainTeam,r.homeTeam,r.awayTeam]:[r[name]];values.forEach(raw=>{const v=String(raw||'').trim();if(!v)return;const k=key(v),old=freq.get(k);freq.set(k,{label:old?.label||v,count:(old?.count||0)+1})})});if(name==='mainTeam'){[form.elements.homeTeam?.value,form.elements.awayTeam?.value].forEach(raw=>{const v=Stri