ng(raw||'').trim();if(v){const k=key(v),old=freq.get(k);freq.set(k,{label:old?.label||v,count:(old?.count||0)+1000})}})}return[...freq.values()].sort((a,b)=>b.count-a.count||a.label.localeCompare(b.label,'pt-BR')).map(x=>x.label)};const close=()=>{menu.classList.remove('show');active=-1};const choose=v=>{input.value=v;input.dispatchEvent(new Event('change',{bubbles:true}));close();input.focus()};const draw=()=>{const q=key(input.value);if(q.length<2){close();return}opts=pool().filter(v=>key(v).startsWith(q)||key(v).includes(' '+q)).slice(0,10);if(!opts.length){close();return}menu.innerHTML=opts.map((v,i)=>`<div class="autocomplete-option" data-i="${i}">${esc(v)}</div>`).join('');menu.classList.add('show');menu.querySelectorAll('.autocomplete-option').forEach(o=>o.onmousedown=e=>{e.preventDefault();choose(opts[Number(o.dataset.i)])})};input.addEventListener('input',draw);input.addEventListener('focus',()=>{if(input.value.trim().length>=2)draw()});input.addEventListener('keydown',e=>{if(!menu.classList.contains('show'))return;if(e.key==='ArrowDown'){e.preventDefault();active=Math.min(opts.length-1,active+1)}else if(e.key==='ArrowUp'){e.preventDefault();active=Math.max(0,active-1)}else if(e.key==='Enter'&&active>=0){e.preventDefault();choose(opts[active]);return}else if(e.key==='Escape'){close();return}else return;menu.querySelectorAll('.autocomplete-option').forEach((o,i)=>o.classList.toggle('active',i===active))});input.addEventListener('blur',()=>setTimeout(close,140))});form.elements.sport?.addEventListener('change',()=>['competition','homeTeam','awayTeam','mainTeam'].forEach(n=>{const el=form.elements[n];if(el)el.dispatchEvent(new Event('input'))}))}function newForm(){metrics(records);$('#ticker').innerHTML='';const r=editing?records.find(x=>x.id===editing):null;$('#content').innerHTML=`<article class="card"><div class="card-head"><div><div class="ey">REGISTRO</div><h3>${r?'Editar':'Nova'} análise</h3></div></div><div class="muted" style="margin:-4px 0 14px;font-size:10px">Os campos de texto sugerem valores já usados após a segunda letra.</div><form id="form" class="formgrid"><div class="field"><label>DATA</label><input name="date" type="date" value="${r?.date||new Date().toISOString().slice(0,10)}" required></div><div class="field"><label>ESPORTE</label><input name="sport" value="${esc(r?.sport||'Futebol')}" required></div><div class="field"><label>COMPETIÇÃO</label><input name="competition" value="${esc(r?.competition||'')}"></div><div class="field"><label>TIME DA CASA</label><input name="homeTeam" value="${esc(r?.homeTeam||'')}"></div><div class="field"><label>TIME VISITANTE</label><input name="awayTeam" value="${esc(r?.awayTeam||'')}"></div><div class="field"><label>TIME PRINCIPAL</label><input name="mainTeam" value="${esc(r?.mainTeam||'')}"></div><div class="field"><label>MERCADO</label><input name="market" value="${esc(r?.market||'')}"></div><div class="field"><label>ESTRATÉGIA</label><input name="strategy" value="${esc(r?.strategy||'')}"></div><div class="field"><label>PLATAFORMA</label><input name="platform" value="${esc(r?.platform||'')}"></div><div class="field"><label>ANALISTA</label><input name="analyst" value="${esc(r?.analyst||'')}"></div><div class="field"><label>VALOR SIMULADO</label><input name="analysisWeight" type="number" step="0.01" inputmode="decimal" value="${r?num(r.analysisWeight).toFixed(2):''}"></div><div class="field"><label>COTAÇÃO</label><input name="confidenceIndex" type="number" step="0.001" inputmode="decimal" value="${r?num(r.confidenceIndex).toFixed(3):''}"></div><div class="field"><label>PRÉ / LIVE</label><select name="timing"><option value="pre" ${!r||r?.timing!=='live'?'selected':''}>PRÉ</option><option value="live" ${r?.timing==='live'?'selected':''}>LIVE</option></select></div><div class="field"><label>RESULTADO</label><select name="result">${[['pending','Pendente'],['success','Acerto'],['failure','Erro'],['void','Anulado'],['manual','Manual']].map(x=>`<option value="${x[0]}" ${r?.result===x[0]?'selected':''}>${x[1]}</option>`).join('')}</select></div><div class="field"><label>RETORNO / IMPACTO</label><input name="impactScore" type="number" step="0.01" value="${r?.impactScore||''}"></div><div class="field full"><label>OBSERVAÇÕES</label><textarea name="notes">${esc(r?.notes||'')}</textarea></div><div class="actions full"><button type="button" id="cancel" class="btn secondary">Cancelar</button><button class="btn primary">Salvar análise</button></div></form></article>`;setupAutocomplete($('#form'));const form=$('#form'),weight=form.elements.analysisWeight,odd=form.elements.confidenceIndex;const fmt=(el,d)=>{if(!el||el.value==='')return;const v=num(el.value);el.value=v.toFixed(d)};weight?.addEventListener('blur',()=>fmt(weight,2));odd?.addEventListener('blur',()=>fmt(odd,3));$('#cancel').onclick=()=>{editing=null;go('dash')};form.onsubmit=e=>{e.preventDefault();fmt(weight,2);fmt(odd,3);const o=Object.fromEntries(new FormData(e.target));o.analysisWeight=num(o.analysisWeight);o.confidenceIndex=num(o.confidenceIndex);o.impactScore=num(o.impactScore);if(o.result==='success')o.impactScore=o.analysisWeight*Math.max(o.confidenceIndex-1,0);else if(o.result==='failure')o.impactScore=-o.analysisWeight;else if(o.result==='pending'||o.result==='void')o.impactScore=0;o.updatedAt=new Date().toISOString();if(r){Object.assign(r,o)}else records.push(norm({...o,id:uid(),createdAt:o.updatedAt}));queue();toast('Análise salva');editing=null;go('history')}}function history(){
  $('#ticker').innerHTML='';
  $('#metrics').innerHTML='<div id="historySummary" class="history-summary full" style="grid-column:1/-1"></div>';
  $('#content').innerHTML=`<article class="card history-shell"><div class="card-head"><div><div class="ey">HISTÓRICO</div><h3>Análises esportivas</h3></div><button id="add" class="btn primary">Nova</button></div><div class="history-toolbar"><input id="q" placeholder="Pesquisar evento, time, anotação ou categoria"><select id="st"><option value="all">Todos os status</option><option value="success">Acertos</option><option value="failure">Erros</option><option value="pending">Pendentes</option><option value="void">Anulados</option><option value="manual">Manual</option></select><select id="sd"><option value="all">Casa e fora</option><option value="home">Casa</option><option value="away">Fora</option></select><select id="tm"><option value="all">PRÉ e LIVE</option><option value="pre">PRÉ</option><option value="live">LIVE</option></select></div><div id="records"></div></article>`;
  $('#add').onclick=()=>go('new');
  const statusLabel={success:'Acerto',failure:'Erro',pending:'Pendente',void:'Anulado',manual:'Manual'};
  let sortKey=null,sortDir=0;
  const text=v=>String(v??'').trim().toLocaleLowerCase('pt-BR');
  const sortValue=(r,key)=>({
    date:r.date,platform:text(r.platform),sport:text(r.sport),event:text((r.homeTeam||'')+' '+(r.awayTeam||'')),mainTeam:text(r.mainTeam),category:text(r.market),timing:r.timing==='live'?1:0,market:text((r.market||'')+' '+(r.strategy||'')),analyst:text(r.analyst),weight:num(r.analysisWeight),odd:num(r.confidenceIndex),result:({success:1,failure:2,pending:3,void:4,manual:5}[r.result]||9),impact:num(r.impactScore),notes:r.notes?text(r.notes):'zzzzzz'
  }[key]);
  const compare=(a,b)=>{const av=sortValue(a,sortKey),bv=sortValue(b,sortKey);let c;if(typeof av==='number'&&typeof bv==='number')c=av-bv;else c=String(av).localeCompare(String(bv),'pt-BR',{numeric:true,sensitivity:'base'});return c*sortDir};
  const th=(label,key)=>`<th class="sortable ${sortKey===key?'active':''}" data-sort="${key}">${label}<span class="sort-ind">${sortKey===key?(sortDir===1?'↑':'↓'):''}</span></th>`;
  function draw(){
    const q=$('#q').value.toLowerCase().trim(),st=$('#st').value,sd=$('#sd').value,tm=$('#tm').value;
    let list=records.filter(r=>(st==='all'||r.result===st)&&(sd==='all'||side(r)===sd)&&(tm==='all'||r.timing===tm)&&[r.homeTeam,r.awayTeam,r.mainTeam,r.market,r.strategy,r.competition,r.platform,r.sport,r.analyst,r.notes,r.timing].join(' ').toLowerCase().includes(q));
    list=sortKey&&sortDir?[...list].sort(compare):[...list].sort((a,b)=>b.date.localeCompare(a.date)||String(b.updatedAt).localeCompare(String(a.updatedAt)));
    const fs=stats(list),done=fs.success+fs.failure,avgOdd=avg(list,r=>r.confidenceIndex),summary=$('#historySummary');
    if(summary)summary.innerHTML=`<article class="hs"><small>Retorno filtrado</small><strong class="${fs.impact>=0?'pos':'neg'}">${money(fs.impact)}</strong><span>${list.length} análise${list.length===1?'':'s'}</span></article><article class="hs"><small>Resultados</small><strong><i class="pos">${fs.success} certas</i> · <i class="neg">${fs.failure} erradas</i> · <i class="amber">${fs.pending} pendentes</i></strong><span>${done} concluída${done===1?'':'s'}</span></article><article class="hs"><small>Taxa de acerto</small><strong class="${fs.rate>=50?'pos':'amber'}">${pct(fs.rate)}</strong><span>${fs.success}/${done||0} concluídas</span></article><article class="hs"><small>ROI / Odd média</small><strong class="${fs.roi>=0?'pos':'neg'}">${pct(fs.roi)}</strong><span>Odd média ${quote(avgOdd)}</span></article>`;
    $('#records').innerHTML=list.length?`<div class="history-table-wrap"><table class="history-table"><thead><tr>${th('Data do encontro','date')}${th('Plataforma','platform')}${th('Esporte','sport')}${th('Evento','event')}${th('Time analisado','mainTeam')}${th('Categoria','category')}${th('Momento','timing')}${th('Mercado / Estratégia','market')}${th('Analista','analyst')}${th('Valor simulado','weight')}${th('Cotação','odd')}${th('Resultado','result')}${th('Retorno simulado','impact')}${th('Anotação','notes')}<th class="no-sort">Ações</th></tr></thead><tbody>${list.map(r=>`<tr><td><b>${r.date.split('-').reverse().join('/')}</b></td><td><b>${esc(r.platform||'Não informado')}</b></td><td><b>${esc(r.sport||'—')}</b></td><td><b>${esc(r.homeTeam||'Casa')} × ${esc(r.awayTeam||'Visitante')}</b><small>${esc(r.competition||'Sem competição')}</small></td><td><b>${esc(r.mainTeam||'Não informado')}</b><small>${side(r)==='home'?'Casa':side(r)==='away'?'Fora':'Não definido'}</small></td><td><b>${esc(r.market||'Geral')}</b></td><td><span class="timing-badge ${r.timing==='live'?'timing-live':'timing-pre'}">${r.timing==='live'?'LIVE':'PRÉ'}</span></td><td><b>${esc(r.market||'Geral')}</b><small>${esc(r.strategy||'Sem estratégia')}</small></td><td><b>${esc(r.analyst||'Sem analista')}</b></td><td><b>${money(r.analysisWeight)}</b></td><td><b>${quote(r.confidenceIndex)}</b></td><td><select class="history-result ${r.result}" data-status="${r.id}">${Object.entries(statusLabel).map(([v,l])=>`<option value="${v}" ${r.result===v?'selected':''}>${l}</option>`).join('')}</select></td><td><b class="${r.impactScore>=0?'pos':'neg'}">${money(r.impactScore)}</b></td><td class="notes-cell"><span title="${esc(r.notes||'Sem anotação')}">${esc(r.notes||'Sem anotação')}</span></td><td><div class="history-actions"><button class="btn secondary icon-btn" title="Editar" data-edit="${r.id}">✎</button><button class="btn danger icon-btn" title="Excluir" data-del="${r.id}">×</button></div></td></tr>`).join('')}</tbody></table></div>`:'<div class="history-empty">Nenhuma análise encontrada.</div>';
    $$('[data-sort]').forEach(h=>h.onclick=()=>{const key=h.dataset.sort;if(sortKey!==key){sortKey=key;sortDir=1}else if(sortDir===1)sortDir=-1;else if(sortDir===-1){sortKey=null;sortDir=0}else{sortDir=1}draw()});
    $$('[data-edit]').forEach(b=>b.onclick=()=>{editing=b.dataset.edit;go('new')});
    $$('[data-del]').forEach(b=>b.onclick=()=>{if(confirm('Excluir esta análise?')){records=records.filter(r=>r.id!==b.dataset.del);queue();history()}});
    $$('[data-status]').forEach(sel=>sel.onchange=()=>{